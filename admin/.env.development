# 开发环境 API 路径
VITE_API_URL=/api/admin

# 开发服务器代理目标 (仅在 vite.config.ts 中使用)
VITE_PROXY_TARGET=http://localhost:6632

# 接口数据混合加密配置（开发环境默认关闭）
# HYBRID=true: RSA公钥加密会话AES密钥 + AES加密数据（推荐）
VITE_ENCRYPTION_HYBRID=false
