# 接口数据加密配置
# 开启后所有业务接口的请求体和响应体会进行 AES-256-GCM 加密传输
ENCRYPTION_ENABLED=false
# 加密密钥（共享密钥模式使用，生产环境建议使用混合模式）
ENCRYPTION_KEY=default-dev-key-change-in-production
# 混合加密模式（公私钥+会话 AES 密钥，推荐生产环境开启）
ENCRYPTION_HYBRID=true
